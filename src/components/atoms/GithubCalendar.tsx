'use server';

import React from 'react';
import GitHubCalendar from 'react-github-calendar';

const GithubCalendar = () => {
  return (
    <GitHubCalendar username='muhammadrayhanr' blockSize={9.174527726415} />
  );
};

export default GithubCalendar;
